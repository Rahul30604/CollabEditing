# Development Profile
# Active by default when no profile is specified

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  devtools:
    restart:
      enabled: true

logging:
  level:
    com.collabeditor: DEBUG
    org.springframework.web: DEBUG
